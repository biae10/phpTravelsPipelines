name: pipeline de estudos
on:  workflow_dispatch

jobs:
  job1:
    runs-on: windows-latest

    steps:
      - name: baixar repositório
        run: ---------- Baixando repositório ---------
      
     - name:
       uses: actions/checkout@v2.4.0
       with: actions/checkout
      